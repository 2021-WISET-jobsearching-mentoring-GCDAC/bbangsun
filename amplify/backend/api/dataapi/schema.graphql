type Hashtag @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
}

type Bakery @model @auth(rules: [{allow: public}]) {
  Name: String
  Num: Int
  location: String
  openDate: String
  openHour: String
  lineUp: String
  lineUpPrice: String
  SNS: String
}
 